[build]
  base = "client"
  command = "rm -rf dist && npm ci && npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[cache]
  skip_caching = false
